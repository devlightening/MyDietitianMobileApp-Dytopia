import React from "react";

// Screens now render their own headers.
// This component is kept as a no-op so existing imports don't break.
export default function AppHeader(_props: any) {
  return null;
}
